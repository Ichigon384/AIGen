# PyTorch:
# - Windows/Linux + NVIDIA GPU: CUDA対応版を別途インストールすることを推奨します。
#   例 (CUDA 12.1): pip install torch --index-url https://download.pytorch.org/whl/cu121
# - Mac (Apple Silicon): 通常の `pip install torch` でMPSバックエンドが使えます（追加設定不要）。
# 詳細はREADME.mdのセットアップ手順を参照してください。
torch>=2.1.0

diffusers>=0.27.0
transformers>=4.38.0
accelerate>=0.27.0
safetensors>=0.4.2
gradio>=4.19.0
Pillow>=10.0.0
imageio>=2.34.0
imageio-ffmpeg>=0.4.9

# iPhone等で撮影されたHEIC/HEIF画像をアップロードできるようにするために使用
pillow-heif>=0.15.0

# オブジェクト生成タブの背景透過処理に使用 (CPU上のonnxruntimeで動作し、GPU種別を問わず利用可能)
# 新しいバージョンのrembgはonnxruntimeを同梱しないため、[cpu]エクストラを明示的に指定する
rembg[cpu]>=2.0.50

# 日本語プロンプトの自動英訳 (Helsinki-NLP/opus-mt-ja-en) のトークナイザに必要
sentencepiece>=0.2.0

# 任意: VRAM節約・高速化 (NVIDIA GPU環境のみ。Macでは不要/非対応)
# xformers
